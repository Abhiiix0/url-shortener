# Base URL the browser uses for API calls. Leave empty in dev: requests go through Vite's proxy.
VITE_API_URL=

# Where the Vite dev proxy forwards /api/* and short-code redirects.
BACKEND_URL=http://localhost:5000
